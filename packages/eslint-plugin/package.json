{
  "name": "eslint-plugin-react-strict-dom",
  "version": "0.0.1",
  "description": "ESLint plugin for React Strict DOM",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "npm run clean",
    "build": "babel --config-file ../../configs/babel.config.js src/ --out-dir dist/ --copy-files",
    "clean": "del-cli \"./dist/*\""
  },
  "dependencies": {
    "hermes-eslint": "^0.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT",
  "repository": {
    "type": "git"
  }
}
